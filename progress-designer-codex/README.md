# Progress Designer (Codex)

Local authoring tool for `data/*.json` focused on unlock-driven progression with resource-informed difficulty hints.

## Run

From repo root:

```bash
npm run dev:progress
```

Then open:

- `http://localhost:3189/`

